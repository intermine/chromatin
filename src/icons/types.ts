import { ThemeCSSStyles } from '../styles'

export interface ChromatinIcon extends React.SVGProps<SVGSVGElement> {
    /**
     * To extend the styles applied to the components
     */
    classes?: {
        /**
         * Applied to root component
         */
        root?: string
    }
    /**
     * To override the applied styles.
     */
    csx?: {
        /**
         * Applied to root component
         */
        root?: ThemeCSSStyles
    }
}
